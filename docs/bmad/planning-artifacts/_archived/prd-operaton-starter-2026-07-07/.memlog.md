---
topic: Examples Gallery: Repository-Wide Descriptor Discovery
updated: 2026-07-07T05:49
---

- (decision) Fast Path chosen: requirements fully specified in invocation args; no discovery turns needed
- (decision) Intent: Create new PRD (parent gallery PRD is already final)
- (decision) GitHub Trees API chosen for whole-repo scan (single round-trip, no auth needed for public repos)
- (decision) path field becomes optional with default '.'; existing root manifests are automatically backward-compatible
- (decision) Both .yml and .yaml processed independently when both present in same dir; warning logged
- (assumption) OQ-1: truncated tree warning+partial processing acceptable for v1 (expected repos are small)
- (assumption) OQ-2: same-dir .yml/.yaml collision: log warning, process both (not an error)
- (decision) OQ-2 resolved: .yml takes precedence over .yaml in same directory; .yaml skipped with warning
- (event) PRD finalized
