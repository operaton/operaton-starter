# Decision Log — operaton-starter PRD

Bootstrapped from commit history on 2026-05-29. Records decisions, changes, and overrides applied after the PRD reached `workflowStatus: complete` (2026-03-27).

---

## 2026-05-29 — Reconcile commits `58c1961`–`9febe99`

### D1: Bundle build tool wrappers in generated projects

**Commit:** `9febe99` — feat: Add Maven/Gradle wrapper

**Decision:** Generated Maven projects now include `mvnw`, `mvnw.cmd`, and `.mvn/wrapper/maven-wrapper.properties`. Generated Gradle projects include `gradlew`, `gradlew.bat`, `gradle/wrapper/gradle-wrapper.jar`, and `gradle/wrapper/gradle-wrapper.properties`. ZIP archives cannot preserve Unix execute permissions, so the generated README instructs users to run `chmod +x mvnw` or `chmod +x gradlew` once after extraction (Unix/macOS only).

**Impact on PRD:** New FR45 added; FR33 and Success Criteria "First-run success" updated; NFR14 clarified.

---

### D2: ProcessIT test scaffold — MockExpressionManager + delegate registration

**Commit:** `03a2b44` — fix: Fix test template

**Decision:** The generated `ProcessIT.java` template now configures `StandaloneInMemProcessEngineConfiguration` with `MockExpressionManager` and registers `SkeletonDelegate` via `Mocks.register` in `@BeforeEach`, resetting in `@AfterEach`. This is required to resolve the `${skeletonDelegate}` expression in the skeleton BPMN service task during the integration test — without it the test fails on first run, breaking FR2 and FR44.

**Impact on PRD:** FR44 updated to reflect mock configuration requirement.

---

### D3: Remove committed JTE-generated classes from version control

**Commit:** `58c1961` — Deleted generated classes

**Decision:** JTE-generated `.java` class files under `starter-templates/src/main/jte-classes/` were being tracked in git. They are now excluded via `.gitignore`. No functional change; no PRD impact.
